ARSENAL_API_TOKEN=dev-token-change-me
# Simple UI panel password (default: 23513900) — same effect as API token
ARSENAL_PANEL_PASSWORD=23513900
PORT=3847
HOST=127.0.0.1
# H4CK_ROOT=/abs/path/to/h4ck

# ── Schrödinger P0 — platform ────────────────────────────────────────────────
# Max concurrent scans (default: 3)
# SCHRODINGER_MAX_CONCURRENT=3

# Allowlist (canonical). Alias: SCHRODINGER_TARGET_ALLOWLIST
# Comma-separated domains / *.suffix globs, or * = all FQDNs (SSRF still blocks private IPs)
# SCHRODINGER_ALLOWLIST=*
# SCHRODINGER_TARGET_ALLOWLIST=*

# Optional extra JSON store path (empty = ScanStore default under backend/data/)
# SCHRODINGER_STORE_FILE=

# Audit ring buffer size (default: 1000)
# SCHRODINGER_AUDIT_LOG_SIZE=1000

# Test-only: delay before runScan so DELETE cancel can win against mock (<1ms)
# SCHRODINGER_SCAN_START_DELAY_MS=0

# ── Schrödinger FEATURE_* flags (GET /api/schrodinger/flags) ─────────────────
# FEATURE_schrodinger_guardrails=true
# FEATURE_schrodinger_persist_postgres=false
# FEATURE_schrodinger_v2_providers=true

# ── Schrödinger P1 — observation ─────────────────────────────────────────────
# live = real dig/HTTP/TCP; mock = fixture path (CI / dig missing / demos)
# SCHRODINGER_SCAN_MODE=live

# DNS provider: auto (dig if present else mock) | dig | mock
# SCHRODINGER_DNS_MODE=auto

# Per-vantage feature flags (default: all four)
# SCHRODINGER_VANTAGES=dns,ua,netweb,time

# Port profile for NetWeb: quick | web
# SCHRODINGER_PORT_PROFILE=quick

# Optional DoH second opinion (Google + Cloudflare) — never replaces dig
# SCHRODINGER_DOH=0

# DNS sampling / pool
# SCHRODINGER_DNS_SAMPLE=30
# SCHRODINGER_DNS_CONCURRENCY=6
# SCHRODINGER_DNS_TIMEOUT_MS=2500
# SCHRODINGER_DNS_RETRIES=1

# UA / HTTP paths (comma-separated)
# SCHRODINGER_UA_PATHS=/,/robots.txt,/wp-admin,/.well-known/security.txt

# Override rules file (default: shared/schrodinger-rules.json)
# SCHRODINGER_RULES_PATH=

# ── Local infra (docker compose -f docker-compose.dev.yml up -d) ─────────────
# SCHRODINGER_POSTGRES_URL=postgresql://schrodinger:schrodinger_dev_pass@127.0.0.1:5432/schrodinger
# REDIS_URL=redis://127.0.0.1:6379
# FEATURE_schrodinger_persist_postgres=true
# FEATURE_schrodinger_watch=true

# Web Push (Secret Manager in GCP)
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=

# ── GCP / Docker production ──────────────────────────────────────────────────
# HOST=0.0.0.0
# PORT=8080
# H4CK_ROOT=/app/h4ck-stub
# On GCE / Cloud Shell / Cloud Run image: dig via dnsutils; or SCHRODINGER_DNS_MODE=mock
# Terraform: see infra/README.md
